NGINX project 
NGINX :  NGINX can receive that request and return the requested page or resource. It is an open-source web server known for high performance, stability, 
and efficient request handling. In practice, NGINX can route traffic to the right file or application, making it useful for serving websites and handling large 
numbers of requests smoothly 

Docker Commands to successful built:
-docker build -t my-nginx .
-docker run -d -p 8080:80 --name nginx-container my-nginx

OPEN BROWSER
 -http://localhost:8080

Display Result 
 - Result got it 
